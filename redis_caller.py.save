import redis
import time
import datetime as dt
import requests
import redis_set2
#print(dt.datetime.utcfromtimestamp(time.time()-14400).strftime("%Y/%m/%d %H:%M")) #hora en stgo
#print(dt.datetime.utcfromtimestamp(time.time()-14400).strftime("%Y/%m/%d %H:%M")) #hora en NY
#print(dt.datetime.utcfromtimestamp(time.time()+7200).strftime("%Y/%m/%d %H:%M")) #hora en Zurich
#print(dt.datetime.utcfromtimestamp(time.time()+36000).strftime("%Y/%m/%d %H:%M")) #hora en Auckland 
#print(dt.datetime.utcfromtimestamp(time.time()-14400).strftime("%Y/%m/%d %H:%M")) #hora en georgia

r=redis.Redis()
def getter(ciudad):
 data=r.hgetall(ciudad)
 print(r.hgetall("zurich"))
 return data
 #return lat,lon

#if __name__ == "__main__":
# redis_set2.set()
# getter("santiago")
